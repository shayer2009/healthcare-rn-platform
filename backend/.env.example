PORT=4000
JWT_SECRET=change_this_secret
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=health_user
DB_PASSWORD=health_pass
DB_NAME=healthcare_app
# Set DB_SSL=true for DigitalOcean managed databases (required)
DB_SSL=false

# Daily.co Video (get API key from https://dashboard.daily.co/)
DAILY_API_KEY=

# Email (SMTP) for reminders
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@healthapp.local

# Twilio SMS for reminders (get from https://www.twilio.com/)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Phase 3: Redis (for caching and rate limiting)
REDIS_URL=redis://localhost:6379
# OR
REDIS_HOST=localhost
REDIS_PORT=6379

# Phase 2: Encryption
ENCRYPTION_KEY=

# Phase 5: Kafka (for event streaming)
KAFKA_BROKERS=localhost:9092

# Logging
LOG_LEVEL=info

# Error Tracking (Sentry)
SENTRY_DSN=

# API URL (for Swagger docs)
API_URL=http://localhost:4000
